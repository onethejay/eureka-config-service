server:
  port: 8888

spring:
  application:
    name: config-service

#  config:
 #   activate:
  #    on-profile: dev

  cloud:
    config:
      server:
        git:
          uri: file:///Users/jeonghun/git-local-repo # Mac ??